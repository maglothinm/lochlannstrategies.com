LOCHLANN STRATEGIES v17.2.2 — DEPLOYMENT

1. Upload the contents of this package to the existing GitHub Pages repository root.
2. Replace prior HTML, sitemap, robots, documentation, and packaged assets.
3. Include sectors.html so the obsolete Sectors URL continues to redirect to Experience.
4. Preserve any repository-only CNAME file.
5. Commit and wait for GitHub Pages to publish the update.
6. Open the Home page in a private Safari window in both portrait and landscape orientation.
7. Confirm that the hero begins with “Lochlann Strategies provides…” and follows with “Every engagement is led directly by Michael Maglothin…”.
8. Confirm that `See capabilities` is the primary action, `Explore the experience` is secondary, and the circular operating map remains absent at every orientation.

The v17.2.2 CSS filename and query string are intentional cache busters and should not be renamed. The unchanged JavaScript remains v17.2.1.
